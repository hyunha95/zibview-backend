package com.view.zib.domain.image.service;

import com.view.zib.domain.image.controller.request.SaveImageRequest;

public interface ImageService {
    void saveImage(SaveImageRequest saveImageRequest);
}
